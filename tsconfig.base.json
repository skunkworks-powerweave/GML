{
  "_comment": "Spec 166 — repo-wide TypeScript baseline. Individual packages should `extends` this and override only what they truly need (e.g. apps/web pins target ES2017 for browser bundle compatibility but otherwise inherits the rest). Adoption across all packages is a follow-up; see specs/166-build-and-config-hygiene/research.md for the rollout plan.",
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "composite": true,
    "strict": true,
    "skipLibCheck": true,
    "esModuleInterop": true,
    "isolatedModules": true,
    "resolveJsonModule": true,
    "forceConsistentCasingInFileNames": true
  }
}
