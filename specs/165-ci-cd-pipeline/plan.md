# Plan 165

CREATED: `specs/165-ci-cd-pipeline/{spec,plan,research,quickstart,tasks}.md`, `tests/governance/test_165_ci_cd_pipeline.test.mjs`, `.github/workflows/test.yml` (single GitHub Actions workflow named `test-and-build`; triggers on push to `main` and on every pull_request; single job runs on `ubuntu-latest` with job-level `DATABASE_URL` env so `packages/db/src/client.ts` loads its build-safe singleton during `pnpm build`; steps in order: `actions/checkout@v4`, `pnpm/action-setup@v4` (version auto-detected from the `packageManager` field in `package.json`, currently pnpm@10.33.4), `actions/setup-node@v4` with `node-version: 22` and `cache: "pnpm"`, `pnpm install --frozen-lockfile`, `pnpm test`, `pnpm build`, `pnpm -r typecheck`, `pnpm test:smoke || true` — the `|| true` keeps the gate lenient when the Docker stack is unreachable on the runner; the smoke tests themselves already `test.skip` on connection refusal per spec 003, the `|| true` is belt-and-suspenders for a runner with no Docker at all), `.github/workflows/README.md` (3-line note explaining the gates)
EDITED: none — this spec is a pure addition of CI infrastructure; no application-side `.ts` / `.tsx` file is touched, no `package.json` script is added (the workflow uses the existing `test`, `build`, `test:smoke` scripts from the root `package.json` and the `typecheck` script from `packages/db/package.json` via the `-r` flag)
MIGRATED: none — no schema delta, next migration idx remains 0021 (assigned to spec 162). No new dependencies in either `package.json` or any workspace `package.json` (the workflow uses GitHub-hosted runners with built-in Node + pnpm via the marketplace actions `pnpm/action-setup@v4` and `actions/setup-node@v4`, both pinned by major version). No env contract change for the application — `DATABASE_URL` is set inside the workflow YAML only and never read at runtime by the application during CI (the build step reads it via the build-safe fallback in `packages/db/src/client.ts` per spec 143). No client component added; no server component touched
