# Tasks 131

- [x] T1 → write governance test (red) covering the new `desc` import in page.tsx, the feedback_responses query keyed by formId+respondentUserId+pairingId, the orderBy desc + limit 1, the `draft ?? prior ?? {}` precedence in initialResponses, the savedIndicator top-of-card placement with `marginLeft: "auto"`, the literal `Save failed — retrying…` copy, the `data-saved-indicator` + `aria-live="polite"` wiring, and the autosave-disabled null return → run the suite → red.
- [x] T2 → edit `apps/web/src/app/(authenticated)/forms/[slug]/page.tsx`: add `desc` to the drizzle-orm import; after the form_drafts lookup, query feedback_responses by formId+respondentUserId+pairingId orderBy desc(submittedAt) limit 1 when pairingId is truthy; compose `initialResponses` as `draft.responses ?? priorResponses ?? {}`; thread it into the FormRenderer mount.
- [x] T3 → edit `apps/web/src/components/forms/FormRenderer.tsx`: move the savedIndicator JSX out of the bottom button row into a top-of-card flex row with `marginLeft: "auto"`; switch the error copy to "Save failed — retrying…" (U+2026); add `data-saved-indicator` + `aria-live="polite"` + var(--mono) typeface; rely on the existing `forceTick` setInterval — do not add a second timer; ensure the `useEffect` cleanup function still calls clearInterval.
- [x] T4 → author all five spec-kit files under specs/131-form-prior-response-and-saved-indicator/.
- [x] T5 → run `pnpm test -- --grep "spec 131"` (or the full suite scoped to test_131_*) and confirm green.
- [ ] T6 (future) → when observation_response_items grows a respondent_user_id index, mirror this prefill path for the spec-075 cycle runner. Currently out of scope because spec 131 deliberately avoids schema additions for Workflow Run 11.
- [ ] T7 (future) → consider surfacing a small "Prefilled from your earlier submission" chip when the prior-response branch fires. Decided against in research §"Trade-off: showing the prefill source" — revisit only if user research surfaces confusion.
