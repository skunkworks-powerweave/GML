# Quickstart 050

Visit `/repo/outlines` → table of every course outline with subject + grade + term + status pill. Click a row → `/repo/outline/[id]` renders the JSX prototype's two-column layout (outcomes/lessons/sessions + details/readings).
