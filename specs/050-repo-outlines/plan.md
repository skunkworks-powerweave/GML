# Plan 050

CREATED: `apps/web/src/app/(authenticated)/repo/outlines/page.tsx`, `apps/web/src/app/(authenticated)/repo/outline/[id]/page.tsx`, `tests/governance/test_050_repo_outlines.test.mjs`
EDITED: none
MIGRATED: none (schema locked; tables `course_outlines` + `outline_lessons` already present per spec 016)
