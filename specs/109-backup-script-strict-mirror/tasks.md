# Tasks 109

- [x] T1 → write governance test (red) → edit `scripts/backup.sh` (drop `|| true` on `mc mirror` line, drop fallback echo on `docker compose cp`, add ERR trap, add `MinIO mirror complete` success log via `du -sh`) → green
