# Plan 170

CREATED: `specs/170-redis-singleton-and-error-pages/{spec,plan,research,quickstart,tasks}.md`, `tests/governance/test_170_redis_singleton_and_error_pages.test.mjs`, `apps/web/src/lib/redis.ts` (process-wide ioredis singleton exporting `getRedis()` and `pingRedis()`; defaults match `apps/worker/src/queues.ts` shape — `maxRetriesPerRequest: null`, `enableReadyCheck: false`, `lazyConnect: true` — so web and worker processes have aligned connection semantics; `pingRedis` is a safe-never-throws liveness probe used by `/api/health`; error strings sliced to 200 chars so a verbose ioredis stack can't blow up the health response body; module-scope `_client` cache so a hot-reloaded import doesn't double the connection count)
EDITED: `apps/web/src/lib/rate-limit.ts` (the local `client()` factory + `_client` variable removed; the `rateLimit()` function calls `getRedis()` from `./redis` instead; JSDoc FAIL-CLOSED block updated to reference `getRedis()` rather than the old `REDIS_URL not set` literal — the singleton defaults to `redis://redis:6379` so the no-env case becomes ECONNREFUSED at first command, still fail-closed, just at a slightly different throw point; behaviour otherwise identical, the 1423 existing tests keep passing), `apps/web/src/lib/health.ts` (`pingRedis()` delegates to the new `pingRedis` export from `./redis`; old `new Redis(url, ...)` + `client.connect()` + `client.quit()` per-probe shape removed — the singleton handles the connection lifecycle; the function still returns `{ ok, detail? }` so the `/api/health` JSON contract is unchanged), `apps/web/src/app/forbidden/page.tsx` (was a 16-line static functional component, now a Server Component that reads `searchParams.reason` and renders one of four copy variants — `locked`/`smtp_unconfigured`/`session_expired`/default; the `locked` branch calls `auth()` to read `session.user.locked_until` and formats a concrete "try again in <N> minutes" hint via `formatTimeRemaining()`, falling back to "1 hour" when the timestamp is absent or in the past; carries `data-testid="forbidden-page"` and `data-reason={reason}` so e2e and governance tests can pin the rendered variant; the primary CTA label and href change per-variant — locked/smtp/session redirect to /login, default redirects to /), `apps/web/src/auth.ts` (adds `class AccountLockedError extends CredentialsSignin` with `code = "account_locked"`; the locked-account branch in `authorize()` now `throw new AccountLockedError()` instead of `return null`; the spec 161 audit row `auth.account.locked_attempt` still fires BEFORE the throw so SM-1 coverage is preserved; the import statement gains `CredentialsSignin` from `next-auth`; the inline comment explains the throw's purpose and how it pairs with the loginAction catch), `apps/web/src/app/login/actions.ts` (the catch block in `loginAction` detects `code === "account_locked"` on the thrown `AuthError` and `redirect()`s to `/forbidden?reason=locked`; imports `redirect` from `next/navigation`; other AuthError causes — bad creds, rate-limit deny, Redis-down — continue to collapse to the existing generic "Invalid email or password" copy on the login page, preserving spec 141 fail-closed contract that we don't leak which of those caused the miss)
MIGRATED: none — no schema delta. `users.locked_until` was added by spec 161 (migration 0020); the forbidden page just READS it for the locked variant. Next migration idx remains 0021 reserved for spec 162. No new dependencies — `ioredis` is already a web + worker peer dep, `next-auth`'s `CredentialsSignin` export is already available in v5.0.0-beta.25 (the version pinned in `apps/web/package.json`). Web-side BullMQ producer connections (`apps/worker/src/queues.ts`'s embedded `IORedis` instance) are NOT routed through the new singleton — BullMQ's contract requires the connection to be private to the Queue/Worker, so the worker file is unchanged; `transcodeQueue.add` and `getJobCounts` callers in the web app inherit the worker's bundled connection by importing the queue module
