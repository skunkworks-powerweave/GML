# Plan 171

CREATED: `specs/171-docs-refresh/{spec,plan,research,quickstart,tasks}.md`, `tests/governance/test_171_docs_refresh.test.mjs` (8+ assertions covering README-IT.md mentions of /admin/quizzes /admin/transcode-jobs /admin/system-settings, the lockout policy strings "5" and "1 hour", the docs/audit-actions.md taxonomy size >= 30 dotted actions, the spec-113 quickstart references to /login/forgot and /admin/transcode-jobs, and the smoke.test.mjs fetch-count >= 12)

EDITED: `README-IT.md` (three new sections appended — "New admin surfaces (post-audit closure)" lists `/admin/quizzes` `/admin/transcode-jobs` `/admin/system-settings` `/admin/whatsapp-log` with a sentence each on what the surface does and which audit actions it emits; "Account lockout policy" documents the 5-strikes-in-1-hour state machine, the super_admin `POST /api/admin/users/[id]/unlock` clear, and the three audit actions `auth.account.locked` `auth.account.locked_attempt` `auth.account.unlocked`; "Password reset flow" documents the SMTP-gated `/login/forgot` surface, the 30-minute token TTL, the 3/hour per-IP rate limit, and the four `auth.password.*` audit actions; the existing env table picks up six new rows for `WORKER_CONCURRENCY` `TZ` `MINIO_BUCKET` `GML_WHATSAPP_NUMBER` `GML_HELPDESK_PHONE` `GML_HELPDESK_EMAIL`, and the SMTP row picks up a one-line note about the forgot-password degradation path), `docs/audit-actions.md` (rewritten wholesale from a 14-prefix overview to a comprehensive taxonomy with 16 active prefix families and one deferred section; each family gets a Markdown table listing every action under that prefix, when it fires, and the metadata captured; 40+ distinct dotted-notation actions documented covering `auth.*` `gate.*` `form.*` `quiz.*` `whatsapp.*` `transcode.*` `admin.row.*` `learners.*` `mentors.*` `resource.*` `video.*` `observation.*` `mentor.*` `teach_back.*` `system_settings.*` `helpdesk.*` `notifications.*` `user_prefs.*` `dashboard.*` `quickfind.*` `audit.*` `anti_download.*`), `specs/113-docker-compose-boot-smoke/quickstart.md` (three new sub-steps 7.5 / 7.6 / 7.7 covering the three new admin surfaces — quizzes catalog, transcode DLQ, system settings — each with the pass/fail criterion + remediation hint; new step 11 covering the password-reset flow with branches for SMTP-configured vs SMTP-unset deployments; the "After all ten steps" sign-off bullet bumped to "After all eleven steps" and the example `steps_passed` array extended to include 7.5 / 7.6 / 7.7 / 11), `tests/integration/smoke.test.mjs` (four new test cases appended — smoke 9 for `/admin/quizzes`, smoke 10 for `/admin/transcode-jobs`, smoke 11 for `/admin/system-settings`, smoke 12 for `/login/forgot`; shared `assertAuthGated()` helper for the redirect-or-403 contract; total fetch-call count is now 12 vs the spec-111 baseline of 8; new probes follow the existing `skipIfUnreachable` pattern so CI without a running stack still passes by skipping; header comment updated from "8 distinct fetches" to "12 distinct fetches" with provenance attribution to spec 171)

MIGRATED: none — pure documentation spec. No schema delta, no `apps/**`, no `packages/**`, no `scripts/**` touched. The next migration index remains where the previous spec left it. No new dependencies. No new audit actions emitted (this spec documents what already exists, it does not introduce new `recordAudit` call sites). Spec 167 / 168 own the future audit-event surface
