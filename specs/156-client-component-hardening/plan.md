# Plan 156

CREATED: `specs/156-client-component-hardening/{spec,plan,research,quickstart,tasks}.md`, `tests/governance/test_156_client_component_hardening.test.mjs`
EDITED: `apps/web/src/components/quickfind/QuickFind.tsx` (createPortal call wrapped in try/catch returning null on hydration mismatch / document.body teardown so a single render miss never crashes the authenticated route tree), `apps/web/src/components/video/HlsPlayer.tsx` (`import("hls.js")` chain now has a `.catch` arm that calls `setError("Failed to load HLS player: " + String(err))` so a bundle-load failure surfaces a clear message instead of a silent black video element), `apps/web/src/components/video/UploadProgress.tsx` (UploadState gains `errorMessage?: string`; both tus-load-failed paths populate it with `"Upload library unavailable. Please try the WhatsApp PRIMARY path instead."`; a `role="alert"` div renders the message inline below the progress bar for the failed row), `apps/web/src/components/AntiDownloadGuard.tsx` (devtools heuristic gets a 1-second debounce — a transient window-maximize / window-restore resize no longer fires a false-positive audit row; the polling tick arms a setTimeout that re-checks the size delta inside the timer, and the delta-cleared branch + the unmount cleanup both clear any pending timer), `apps/web/src/components/help/HelpPanel.tsx` (the `?` shortcut handler swaps the narrow `tag === "INPUT"`-style branch for `target?.closest('input, textarea, [contenteditable="true"]')` so nested contenteditable widgets correctly suppress the shortcut)
MIGRATED: none — all five fixes are purely client-side; no API, no migrations directory delta (next idx 0018 still reserved for spec 153). No new dependencies (no error-boundary library, no debounce util — the LMS already has hand-rolled debounce). No env contract. No schema delta
