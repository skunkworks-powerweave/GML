# Plan 145

CREATED: `specs/145-signed-url-subnet-binding/{spec,plan,research,quickstart,tasks}.md`, `tests/governance/test_145_signed_url_subnet_binding.test.mjs` (six+ assertions covering: ipToBindKey export, IPv4 /24 collapse, IPv6 /64 expansion + collapse, intra-/24 round-trip success, cross-/24 round-trip rejection with `ip_mismatch`, timingSafeEqual on the bind-key compare, the media-proxy callsite contract that ipToBindKey is imported and applied to the request IP before the verify call)
EDITED: `apps/web/src/lib/video/signed-url.ts` (adds `ipToBindKey` export; signMediaToken HMACs the bind key instead of the raw IP; verifySignedToken generalises the colon-split parse to handle IPv6 bind keys — anchors on fixed slots bucket/objectKey/userId/exp, treats everything in between as the bind key; compares bind keys via the existing `timingSafeEqual` helper; file header documents the /24-vs-/32 trade-off), `apps/web/src/app/api/media/[token]/route.ts` (imports `ipToBindKey`, passes `ipToBindKey(ip)` to `verifySignedToken`, adds a `// Spec 145` comment block at the callsite explaining the cellular-handoff motivation so an operator reading the request handler doesn't have to chase through the helper)
MIGRATED: none — the bind key lives entirely inside the signed token payload, no schema involvement; no new dependencies (pure string manipulation, node-stdlib only); existing phase-5 governance test (`test_phase5_video.test.mjs`) still asserts `signMediaToken`, `verifySignedToken`, `ip_mismatch`, `createHmac` and keeps passing because the reason code is unchanged — only the granularity of the comparison shifts from /32 to /24 (IPv4) and /64 (IPv6)
