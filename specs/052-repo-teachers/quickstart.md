# Quickstart 052

`/repo/teachers` renders the teacher index table (matches `repository.jsx` RepoTeachersIndex); clicking a row opens `/repo/teacher/[id]` rendering the detail layout (matches RepoTeacherPage).
