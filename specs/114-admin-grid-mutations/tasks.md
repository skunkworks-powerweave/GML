# Tasks 114
test (red) → updateRowAction in actions.ts → row-form edit-mode support → delete-button confirm wrapper → page.tsx edit panel + filter toolbar + per-row Edit/Delete + filter-aware pagination → green → commit.
