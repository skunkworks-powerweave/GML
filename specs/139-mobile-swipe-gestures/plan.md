# Plan 139

CREATED: `specs/139-mobile-swipe-gestures/{spec,plan,research,quickstart,tasks}.md`, `apps/web/src/lib/use-swipe.ts` (vanilla React hook — pointer events, reduced-motion subscription, SSR-safe), `apps/web/src/components/shells/MobileDetailSwipeRegion.tsx` (tiny client wrapper that calls `useSwipe` and wires onSwipeRight to `router.back()` with a `router.push(backHref)` fallback for deep links), `tests/governance/test_139_mobile_swipe_gestures.test.mjs`
EDITED: `apps/web/src/components/shells/MobileDetailFrame.tsx` (wraps the entire frame return in `MobileDetailSwipeRegion`; back-arrow tap target is unchanged so the gesture is purely additive), `apps/web/src/components/forms/MobileFormRunner.tsx` (imports `useSwipe`, wires left-swipe to `goNext` and right-swipe to `goPrev`, attaches the returned ref to the outermost div, sets `touchAction: "pan-y"` so vertical scroll still works inside the form body, exposes `data-reduced-motion` for downstream animation throttling)
MIGRATED: none — swipe state is pure browser-side; the server never sees it. No new dependencies (no react-use-gesture, no react-spring, no framer-motion). No env contract. The hook subscribes to `(prefers-reduced-motion: reduce)` via the standard `matchMedia` API so accessibility tooling already in the chrome (spec 026 et seq.) keeps working without extra wiring
