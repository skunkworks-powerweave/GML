# Tasks 097

- [x] T1 → write governance test (red) → author `apps/web/src/app/api/teach-back/[id]/review/route.ts` with POST + GET 405 stub, auth/role gates, scoped UPDATE with `.returning`, and `recordAudit` hook → green
