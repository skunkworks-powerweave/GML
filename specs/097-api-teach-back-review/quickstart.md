# Quickstart 097

Log in as mentor/observer/programme_admin/super_admin → visit `/rtt/teach-back` → click any pending row → press "Mark reviewed" → row's `video_submissions.status` flips to `'reviewed'` and an `audit_log` row appears with `action='teach_back.reviewed'`; subsequent visits show the lichen "Already reviewed" pill.
