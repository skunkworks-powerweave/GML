# Quickstart 069

Sign in as any teacher → visit `/uploads` → the page lists that teacher's own most recent 50 `video_submissions` joined with `files.original_filename` and (when bound) `observation_cycles.code`; dropping a new file on the inline `<UploadProgress />` tray surfaces a progress row immediately and the table updates on next refresh once tusd finalises.
