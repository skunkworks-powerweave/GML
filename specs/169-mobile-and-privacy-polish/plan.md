# Plan 169

CREATED: `specs/169-mobile-and-privacy-polish/{spec,plan,research,quickstart,tasks}.md`, `tests/governance/test_169_mobile_and_privacy_polish.test.mjs`, `apps/web/src/lib/env.ts` (server-side validation helper exporting `assertEnv()` + the `EnvSummary` / `EnvCheck` types; validates `GML_WHATSAPP_NUMBER` and `GML_HELPDESK_PHONE` against `/^\+\d{8,15}$/`, `GML_HELPDESK_EMAIL` against a simple email regex; in production any set-but-invalid value triggers a SEVERE `console.error` line and the validated field reads as null; the function NEVER throws — soft-fail by design so a typo'd helpdesk phone cannot break the authenticated route tree), `apps/web/src/components/nav/SignOutButton.tsx` ('use client' island around the Topbar sign-out form's submit button; onClick invokes `clearAllQuickFindRecents()` synchronously before the native form-submit fires; the wipe is best-effort and never blocks sign-out)
EDITED: `apps/web/src/components/quiz/MobileQuizRunner.tsx` (imports `useSwipe` from `@/lib/use-swipe`; new `goNext` / `goPrev` named helpers replace the inline onClick bodies so a swipe + the button share one code path; the root container picks up `ref={swipeRef}` plus `touchAction: pan-y` so vertical scroll stays native; the Spec 169 inline comment explains why submit is button-only — a swipe must never finalise the quiz), `apps/web/src/components/quickfind/QuickFind.tsx` (new exported `clearAllQuickFindRecents()` helper walks `window.localStorage`, collects every key starting with `gml.quickfind.recent.`, and `removeItem`s each; the try/catch swallows quota / private-browsing / disabled-storage so the helper can be called from the sign-out path without risk of blocking the form submit), `apps/web/src/components/nav/Topbar.tsx` (imports `SignOutButton`, swaps the inline `<button type="submit">` inside the user-pill sign-out form for `<SignOutButton title={...} style={...}>`; the form's server-action signOut stays untouched), `apps/web/src/app/(authenticated)/layout.tsx` (imports `assertEnv` from `@/lib/env`, calls it once per render, uses the validated `helpdeskPhone` / `helpdeskEmail` values for the HelpPanel contact prop; legacy `WHATSAPP_PHONE_NUMBER_ID` / `SMTP_FROM` fallbacks preserved for backwards compat with deployments that haven't migrated to the GML_HELPDESK_* names), `apps/web/src/components/help/HelpPanel.tsx` (new `isUsableWhatsappContact()` predicate gates the wa.me href; when the predicate rejects, the WhatsApp action row is HIDDEN entirely rather than rendered as a disabled stub — a broken wa.me link is worse than no link), `apps/web/src/components/video/UploadModal.tsx` (new `isUsableWhatsappPhone()` predicate gates the entire `Path 1 — WhatsApp (PRIMARY for teachers)` section; when the env value is invalid or missing the section is removed from the DOM and the direct-browser-upload section is the only path), `apps/web/src/app/(authenticated)/videos/page.tsx` + `apps/web/src/app/(authenticated)/uploads/page.tsx` (each now reads the validated `assertEnv().whatsappNumber.value` rather than `process.env.GML_WHATSAPP_NUMBER` directly so a typo'd value falls through to `WHATSAPP_PHONE_NUMBER_ID` / null), `apps/web/src/i18n/locales/en.json` (adds the `login.forgot.*` / `login.reset.*` / `forbidden.*` nested key tree), `apps/web/src/i18n/locales/hi.json` (mirrors with Devanagari translations), `apps/web/src/i18n/locales/bo.json` (mirrors with empty-string placeholders awaiting a Ladakhi translator), `apps/web/src/i18n/config.ts` (`loadMessages` extended with a `mergeRecursive` helper that walks nested objects, treats an empty target string as MISSING so the English fallback wins, and emits a `console.warn` per gap when `NODE_ENV !== 'production'`)
MIGRATED: none — every change is logic / UI / configuration; no API delta, no DB schema delta. Next migration idx remains unchanged. No new dependencies (the swipe hook, the localStorage helper, the env validation regexes, and the i18n fallback walker are all hand-rolled / re-using existing modules)
