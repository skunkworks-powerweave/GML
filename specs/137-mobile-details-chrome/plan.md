# Plan 137

CREATED: `specs/137-mobile-details-chrome/{spec,plan,research,quickstart,tasks}.md`, `apps/web/src/components/shells/MobileDetailFrame.tsx`, `tests/governance/test_137_mobile_details_chrome.test.mjs`
EDITED: `apps/web/src/components/shells/index.ts` (re-export MobileDetailFrame + MobileDetailFrameProps), `apps/web/src/app/(authenticated)/mentorship/[pairingId]/page.tsx` (conditional MobileDetailFrame wrap with mentor↔teacher title and backHref=/mentorship), `apps/web/src/app/(authenticated)/observation/[cycleId]/page.tsx` (conditional MobileDetailFrame wrap, migrate Sign-off CTA to stickyAction when canSignOff), `apps/web/src/app/(authenticated)/repo/school/[id]/page.tsx` (conditional wrap with school.name title and backHref=/repo/schools), `apps/web/src/app/(authenticated)/repo/class/[id]/page.tsx` (conditional wrap with Grade-n title and back-to-parent-school href), `apps/web/src/app/(authenticated)/repo/teacher/[id]/page.tsx` (conditional wrap with teacher fullName title and backHref=/repo/teachers)
MIGRATED: none — the frame is pure UI composition over existing primitives; getDeviceType() already exists from spec 026, env(safe-area-inset-*) is a CSS env() function (no JS dep), and MobileDetailSwipeRegion + lib/use-swipe are the spec 139 client islands the frame composes against. No schema, no API route, no env addition, no new dependency.
