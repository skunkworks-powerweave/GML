# Plan 153

CREATED: `specs/153-repo-fixes-and-pairings-index/{spec,plan,research,quickstart,tasks}.md`, `packages/db/src/migrations/0018_index_mentor_pairings_teacher.sql` (single-statement CREATE INDEX on mentor_pairings.teacher_id with rationale-bearing header comment explaining why the existing compound unique can't satisfy "by-teacher" lookups), `packages/db/src/migrations/meta/0018_snapshot.json` (cumulative snapshot carrying forward 0017 plus the new mentor_pairings_teacher_idx entry; prevId chains off 0017's id), `tests/governance/test_153_repo_fixes_and_pairings_index.test.mjs` (≥8 assertions: spec-kit presence, mentor detail imports `and` and uses `eq(mentors.active, true)`, sessions page declares `parseIsoDateFilter` with regex + Date + round-trip equality, schema declares `mentor_pairings_teacher_idx`, migration SQL contains CREATE INDEX, snapshot declares the index, journal entry present with idx=18, file shape pins for "Spec 153" comment markers)
EDITED: `apps/web/src/app/(authenticated)/repo/mentor/[id]/page.tsx` (imports `and` from drizzle-orm; mentor SELECT WHERE clause becomes `and(eq(mentors.id, id), eq(mentors.active, true))` so a soft-retired mentor 404s consistently with the index page's `eq(mentors.active, true)` filter; header comment carries spec 153 reference), `apps/web/src/app/(authenticated)/repo/sessions/page.tsx` (adds `parseIsoDateFilter` helper that layers `new Date(value + "T00:00:00Z")` + `!isNaN` + ISO round-trip equality check on top of the existing ISO_DATE_RE regex so a malformed value like 2026-13-45 silently drops the filter instead of throwing a Postgres date-cast error and surfacing as a 500; both fromFilter and toFilter are derived via the helper), `packages/db/src/schema/mentorship.ts` (declares `index("mentor_pairings_teacher_idx").on(t.teacherId)` inside the mentorPairings constraint array, slotted between mentor_pairings_status_idx and the CHECK constraints to preserve declaration order with the snapshot; inline comment notes that the existing compound unique's leading column is mentor_id so a "by-teacher" lookup couldn't use it), `packages/db/src/migrations/meta/_journal.json` (adds idx=18 entry for 0018_index_mentor_pairings_teacher with a sequential `when` timestamp)
MIGRATED: `0018_index_mentor_pairings_teacher` — single CREATE INDEX statement on mentor_pairings(teacher_id). Backwards-compatible (CREATE INDEX adds a btree on an existing column, takes single-digit ms at the current 120-row scale, no data backfill needed). drizzle-kit wraps the statement in a transaction so we deliberately do NOT use CONCURRENTLY (incompatible with transactional DDL). No new dependencies. No env contract change. The fix layers on top of the existing mentor_pairings_mentor_teacher_started_uq compound unique (leading column mentor_id) and mentor_pairings_status_idx — the new index is independent of both
