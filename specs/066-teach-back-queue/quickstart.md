# Quickstart 066
Visit `/rtt/teach-back` as a mentor/observer/programme_admin — the queue lists teach-back submissions; clicking a row populates the right pane with metadata + "Mark reviewed" form pointed at `/api/teach-back/[id]/review`.
