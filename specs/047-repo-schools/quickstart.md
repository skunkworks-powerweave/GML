# Quickstart 047
`/repo/schools` renders the schools index (mirrors prototype lines 152-221); `/repo/school/[id]` renders the detail (lines 226-314); `pnpm test -- tests/governance/test_047_*.test.mjs` green.
