# Tasks 105

- [x] T1 → write governance test (red) → add `@gml/worker` workspace dep + `exports` field → import `transcodeQueue` in webhook route → enqueue after insert + audit → drop deferred-stub comment → green
