# Plan 143

CREATED: `specs/143-schema-cleanup-fk-check-singleton/{spec,plan,research,quickstart,tasks}.md`, `packages/db/src/migrations/0016_schema_cleanup.sql` (hand-crafted migration adding the missing observation_evidence → video_submissions FK and tightening the transcode_jobs.profile CHECK to 480p only), `packages/db/src/migrations/meta/0016_snapshot.json` (chained off the 0015 snapshot id, declares both the new FK and the tightened CHECK), `tests/governance/test_143_schema_cleanup_fk_check_singleton.test.mjs` (8+ assertions pinning the FK + CHECK + journal chain + schema TS + seed.ts removal)
EDITED: `packages/db/src/schema/observation.ts` (added import of videoSubmissions and .references(() => videoSubmissions.id, { onDelete: "set null" }) on videoSubmissionId), `packages/db/src/schema/videos.ts` (tightened the transcode_jobs_profile_check CHECK from IN ('480p','720p') to IN ('480p')), `packages/db/src/scripts/seed.ts` (removed bootstrapSystemSettings helper and its main() call; migration 0015 alone owns the singleton bootstrap to eliminate the race), `packages/db/src/migrations/meta/_journal.json` (inserted idx 16 entry between 15 and 17 so the chain is 0015 → 0016 → 0017), `packages/db/src/migrations/meta/0017_snapshot.json` (prevId updated to chain off 0016's new id and observation_evidence FK + transcode_jobs CHECK rolled forward so the snapshot is consistent), `tests/governance/test_124_system_settings_and_tweaks_admin.test.mjs` (swapped the "seed.ts ships bootstrapSystemSettings" positive assertion for the inverse — the bootstrap was removed in spec 143; the singleton INSERT in migration 0015 is now the single source of truth)
MIGRATED: 0016 (schema_cleanup) — two ALTER TABLE statements, no data backfill needed since neither observation_evidence nor transcode_jobs have any rows that violate the new constraints. The chain is 0015 → 0016 → 0017. No new dependencies. No env contract change. The audit-recovery hook for an accidentally truncated system_settings table is now `pnpm db:migrate` (which re-applies 0015's idempotent INSERT) rather than `pnpm db:seed`
