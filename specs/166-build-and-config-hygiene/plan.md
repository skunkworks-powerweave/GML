# Plan 166

CREATED: `specs/166-build-and-config-hygiene/{spec,plan,research,quickstart,tasks}.md`, `tests/governance/test_166_build_and_config_hygiene.test.mjs`, `.gitattributes` (repo-root file declaring `* text=auto eol=lf` as the default so Windows + OneDrive contributors stop polluting diffs with CRLF/LF churn; plus explicit `binary` pins for `*.pdf`, `*.png`, `*.jpg`, `*.jpeg`, `*.mp4`, `*.mov`, `*.webm`, `*.ico`, `*.woff`, `*.woff2`, `*.ttf`, `*.zip`, `*.gz` so Git stops trying to auto-detect / diff / auto-merge them), `apps/worker/eslint.config.mjs` (minimal flat-config ESLint setup wiring `@typescript-eslint/parser` + `@typescript-eslint/eslint-plugin` with the recommended rule set; brings the worker into the repo-wide `pnpm -r lint` surface that previously skipped it silently), `packages/db/eslint.config.mjs` (identical shape to the worker config; same reasoning — pure TS / Node, no React, no JSX, so `@typescript-eslint/recommended` is exactly the right surface), `tsconfig.base.json` (repo-root shared TypeScript baseline declaring target ES2022, module ESNext, moduleResolution Bundler, composite true, strict true, skipLibCheck true, esModuleInterop true, isolatedModules true, resolveJsonModule true, forceConsistentCasingInFileNames true — adopted by `apps/web` in this run as proof-of-life, with rollout to the other three packages tracked as a follow-up in research.md)

EDITED: `.env.example` (six new env-var documentation lines added in two new comment-bracketed sections — `WORKER_CONCURRENCY=2` and `TZ=Asia/Kolkata` under "Worker / scheduler", and `GML_WHATSAPP_NUMBER`, `GML_HELPDESK_PHONE`, `GML_HELPDESK_EMAIL` under "WhatsApp helpdesk / ingest UX"; the comment-on-the-right style matches the existing file convention so a contributor copying the file to `.env` sees the same shape they're used to; `MINIO_BUCKET=gml-media` was already present in the file from earlier work and is verified as a checkpoint by the governance test), `package.json` (three new root-level scripts added: `"typecheck": "pnpm -r --if-present typecheck"` using the same `-r --if-present` shape as `lint` and `build` for graceful no-op on packages without the script; `"migrate": "pnpm --filter @gml/db migrate"` because only `@gml/db` owns the live database surface; `"seed:all": "pnpm --filter @gml/db seed:all"` same reasoning — brings the root-level surface in line with the existing `lint`/`build`/`test` forwarders), `apps/worker/package.json` (two new devDependencies — `@typescript-eslint/eslint-plugin@^8.18.0` and `@typescript-eslint/parser@^8.18.0` — the only two new packages this spec introduces), `packages/db/package.json` (identical two devDependencies added; same versions, same reasoning), `apps/web/tsconfig.json` (adds `"extends": "../../tsconfig.base.json"` at the top of the file as proof-of-life adoption of the base; the web app overrides `target` ES2017 for browser bundle compatibility and `composite: false` because Next.js manages its own incremental build inside its own `compilerOptions` block; the other tsconfig keys are inherited from the base — `lib`, `paths`, `jsx`, `plugins`, `allowJs`, `incremental`, `noEmit`, `include`, `exclude` remain web-specific overrides)

MIGRATED: none — every change in this spec is a config / docs / hygiene fix. No DB schema delta (next migration idx unchanged), no API change, no behaviour change. The only new dependencies are the two `@typescript-eslint` packages required by the new lint configs. Adopting the new tsconfig base across `apps/worker`, `packages/db`, `packages/shared`, and `packages/ui` is a tracked follow-up — those packages may have package-specific overrides (e.g. `outDir`, `rootDir`) that need case-by-case consideration before the switch
