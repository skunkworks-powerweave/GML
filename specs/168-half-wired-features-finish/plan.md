# Plan 168

CREATED: `specs/168-half-wired-features-finish/{spec,plan,research,quickstart,tasks}.md`, `tests/governance/test_168_half_wired_features_finish.test.mjs`, `apps/web/src/lib/system-settings.ts` (server-only React.cache'd loader for the singleton system_settings row; SELECTs by SYSTEM_SETTINGS_ID with limit 1; returns null on DB error so callers can fall back to schema defaults without crashing the page; consumed by apps/web/src/app/(authenticated)/admin/page.tsx for programmeName + academicYear, apps/web/src/lib/chrome-counts.ts loadUnreadNotifications for notificationsEnabled jsonb-array filter, apps/web/src/app/(authenticated)/videos/page.tsx for videoDefaultQuality passed to UploadModal), `apps/web/src/app/login/forgot/ForgotPasswordForm.tsx` (client component extracted verbatim from the previous /login/forgot page; "use client" island carrying the form state, the fetch call to /api/auth/forgot-password, and the 429 / success surface — behavioural contract preserved byte-for-byte so the spec-161 no-enumeration shape still holds)

EDITED: `apps/web/src/lib/audit.ts` (new recordAuditDedup helper accepting AuditInput + dedupKey + ttlSeconds; SELECTs audit_log filtered by action + userId + metadata `->> '__dedupKey'` + createdAt gte cutoff; on match returns false without inserting, on miss INSERTs with the dedupKey stamped into metadata.__dedupKey; non-atomic on purpose — a few duplicates per hour is fine, the goal is "no 600 rows / minute" not "exactly one"), `apps/web/src/components/video/UploadModal.tsx` (new videoDefaultQuality prop surfaced in the browser-upload explainer card via data-testid="upload-quality-explainer"; defaults to "480p" when null so the modal never renders a blank quality string), `apps/web/src/lib/chrome-counts.ts` (loadUnreadNotifications now consults system_settings.notificationsEnabled and filters via inArray(notifications.kind, enabledKinds); enabledKinds=null → unfiltered fallback, enabledKinds=[] → return 0 without a DB call to avoid Drizzle's empty-IN error), `apps/web/src/app/(authenticated)/admin/page.tsx` (page header now reads programmeName + academicYear from getSystemSettings() with data-testid anchors; falls back to "Goldenmile RTT" / "2026-27" schema defaults when the row is missing), `apps/web/src/app/login/forgot/page.tsx` (rewritten as a server component that reads process.env.SMTP_HOST at render time; when unset, renders a saffron-soft banner with data-testid="forgot-password-smtp-unavailable"; when set, delegates to the new ForgotPasswordForm client island; /api/auth/forgot-password POST still returns 200 in both branches so the no-enumeration contract holds), `apps/web/src/app/(authenticated)/admin/transcode-jobs/page.tsx` (explicit redisUnavailable boolean drives a saffron-soft banner with data-testid="dlq-redis-down-banner" ABOVE the depth strip and table; role="alert" so screen readers announce it on page load; banner copy pins the literal "Live queue depth unavailable" + "historical job table below is still accurate" phrases), `apps/web/src/app/(authenticated)/repo/students/page.tsx` (adds ?q= ILIKE on learners.name with 200-char cap + escapeIlike for \%_; combines with the existing school filter via and(...); calls recordAuditDedup with action="learners.search", entityType="all", dedupKey=`q=${qFilter}|user=${userId}`, ttlSeconds=3600 so the audit log doesn't flood when a user types incrementally — one row per (user × query × hour); the existing learners.bulk_view audit row still fires every render), `tests/governance/test_158_repo_search_bars.test.mjs` (loosened the /repo/students exclusion assertion — the page may carry ilike(learners.name, …) ONLY when recordAuditDedup is also called; a future contributor adding the search without the dedup helper still gets a red light first)

MIGRATED: none — no DB schema delta. The system_settings, audit_log, and notifications tables are unchanged; we just consume existing columns differently (system_settings.programmeName/academicYear/videoDefaultQuality/notificationsEnabled all read from the singleton row; audit_log.metadata picks up a reserved `__dedupKey` field for the dedup logic). Next migration idx remains whatever spec 161 / 162 / 169 reserved. No new dependencies — all four features use React.cache + drizzle's existing `inArray` / `ilike` / `gte` builders + the new recordAuditDedup helper that lives in apps/web/src/lib/audit.ts alongside recordAudit
