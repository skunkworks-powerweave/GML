# Tasks 110

- [x] T1 → write governance test (red) → add `pingMigrations` to `health.ts` with journal read + Drizzle table query + table-not-found sentinel → wire migrations into `/api/health` Promise.all and ok conjunction → green
