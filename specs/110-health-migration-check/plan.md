# Plan 110

CREATED: `specs/110-health-migration-check/{spec,plan,research,quickstart,tasks}.md`, `tests/governance/test_110_health_migration_check.test.mjs`
EDITED: `apps/web/src/lib/health.ts` (export `pingMigrations` + `MigrationsResult` type), `apps/web/src/app/api/health/route.ts` (add migrations to Promise.all + response + overall ok conjunction)
MIGRATED: none
