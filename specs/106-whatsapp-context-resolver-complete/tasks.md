# Tasks 106

- [x] T1 → write governance test (red) → add `mentorMeetings` import + `UUID_RE` regex → replace single OBS branch with full OBS/TB/MM/generic resolver block (fallthrough on miss + `whatsapp.context.unmatched` audit with reason discriminator) → green
