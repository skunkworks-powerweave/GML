# Plan 128

CREATED: `specs/128-chrome-dynamic-counts-and-bell/{spec,plan,research,quickstart,tasks}.md`, `tests/governance/test_128_chrome_dynamic_counts_and_bell.test.mjs`, `apps/web/src/lib/chrome-counts.ts`, `apps/web/src/app/api/notifications/unread-count/route.ts`
EDITED: `apps/web/src/app/(authenticated)/layout.tsx` (load navCounts + unreadCount + queueDepth in parallel via React.cache loaders, forward into shell), `apps/web/src/components/shells/DesktopShell.tsx` (new optional props navCounts/unreadCount/queueDepth, forward to Sidebar + Topbar), `apps/web/src/components/shells/MobileShell.tsx` (new optional props navCounts/unreadCount, forward to BottomTabs), `apps/web/src/components/nav/Sidebar.tsx` (accept counts prop; merge via applyNavCounts before render), `apps/web/src/components/nav/BottomTabs.tsx` (accept counts + unreadCount; render dot badges on observe/pairings/inbox tabs), `apps/web/src/components/nav/Topbar.tsx` (replace bell button with Link href=/inbox + unread chip; add queue indicator chip; drive both off props)
MIGRATED: none — every count lands on an existing schema (mentor_pairings, observation_cycles, video_submissions, form_drafts, notifications) and an existing BullMQ queue (transcode). No new tables, no new env vars.
