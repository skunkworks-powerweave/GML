# Quickstart 014

```powershell
pnpm --filter @gml/db generate   # produces 0002_*.sql
pnpm test                        # green
```

Then `/admin/data/subjects` lets a programme_admin add the curriculum spine.
