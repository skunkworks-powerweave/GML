# Plan 149

CREATED: `specs/149-mobile-runners-async-fixes/{spec,plan,research,quickstart,tasks}.md`, `tests/governance/test_149_mobile_runners_async_fixes.test.mjs`
EDITED: `apps/web/src/components/forms/MobileFormRunner.tsx` (onSubmitClick is now async and awaits flushSave before requestSubmit — mirrors FormRenderer's onSubmitForm closure from spec 072, closes the autosave/submit race that allowed a slow-link refresh mid-submit to resurrect a stale draft), `apps/web/src/components/video/MobileUploadRunner.tsx` (adds mountedRef + redirectTimerRef closure-discipline guards; every tus callback short-circuits on unmount; onSuccess wraps router.push in a try/catch and stores the 1200 ms timer handle so unmount can cancel it; unmount cleanup now flips the mount flag, clears the timer, sets `"Upload cancelled — try again"` error message BEFORE aborting the tus instance, then aborts and nulls the upload ref)
MIGRATED: none — the races are purely client-side; no server-side autosave or tus-endpoint code changed. No new dependencies (no abort-controller polyfill, no react-use-async). No env contract. No schema delta — the migrations directory next-idx (0016) is reserved for a spec that actually touches the DB
