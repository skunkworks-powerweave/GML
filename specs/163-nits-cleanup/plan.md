# Plan 163

CREATED: `specs/163-nits-cleanup/{spec,plan,research,quickstart,tasks}.md`, `tests/governance/test_163_nits_cleanup.test.mjs`, `apps/worker/src/log.ts` (structured-logger helper exporting `log.info` / `log.warn` / `log.error`; emits `[worker][<iso-timestamp>][<level>] <message> <fields-json>` to stderr; circular-reference defensive `JSON.stringify` wrapper so a misbehaving caller can't crash the worker process; used by `apps/worker/src/index.ts` to replace the previous ad-hoc console.log / warn / error scatter)
EDITED: `apps/worker/src/index.ts` (every `console.log` / `console.warn` / `console.error` call replaced with the matching `log.info` / `log.warn` / `log.error` from `./log.js`; sub-tags like `[retention]` become structured fields rather than literal string prefixes; inline Spec 163 comment near the top of the file notes the contract), `apps/web/src/middleware.ts` (stale "once the auditing middleware lands in spec 010" TODO removed; replaced with up-to-date status note pointing readers at the `recordAudit` calls in `apps/web/src/app/api/**` where SM-1 coverage actually lives today; inline Spec 163 comment marks the cleanup), `apps/web/src/lib/rate-limit.ts` (multi-paragraph JSDoc block at the top of the module documents the FAIL-CLOSED contract — `rateLimit()` THROWS on Redis outage rather than returning `{ ok: true }`, callers MUST treat the throw as "denied" not "allowed", with a worked example showing the correct try/catch shape; pins the literal phrase "FAIL-CLOSED" so a future contributor can't silently downgrade), `packages/db/src/scripts/retention.ts` (new `isDirectInvocation()` helper wraps the entry-point guard; primary check is the original strict-path equality, fallback is a basename comparison so pnpm-symlinked / container-bind-mounted invocations still auto-run when called directly; `fileURLToPath` and `basename` imported from node stdlib; inline Spec 163 comment notes the fallback is defensive only)
MIGRATED: none — all five NITS are documentation / structure / format changes; no API delta, no DB schema delta (next migration idx remains 0019 reserved for spec 159 / 0020 reserved for spec 161). No new dependencies (logger is a hand-rolled `process.stderr.write` shim; retention fallback uses node stdlib only). `apps/web/src/components/nav/Topbar.tsx` is touched only by VERIFICATION in the governance test (no hardcoded "EN" remains since spec 155); no source edit
