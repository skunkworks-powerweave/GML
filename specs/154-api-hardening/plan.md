# Plan 154

CREATED: `specs/154-api-hardening/{spec,plan,research,quickstart,tasks}.md`, `tests/governance/test_154_api_hardening.test.mjs`
EDITED: `apps/web/src/app/api/uploads/tus/route.ts` (every method handler now gates on `auth()` via the new `requireAuth()` helper and rejects anonymous callers with 401; the 501 branch routes through a new `tusdUnavailable()` helper that drops the `TUSD_INTERNAL_URL` hint from the response body and logs the diagnostic to `console.warn` for ops; GET and DELETE handlers are added so the auth gate covers every method an attacker can probe), `apps/web/src/app/api/form-drafts/[id]/route.ts` (the PUT handler's silent `.catch(() => ({}))` JSON parse swallow is replaced with an explicit try/catch that returns 400 `{ error: "invalid_json", message: String(err) }` so wire-level failures are distinguishable from schema-level ones), `apps/web/src/app/api/helpdesk/tickets/route.ts` (imports `rateLimit` from `@/lib/rate-limit`; throttles ticket creation at 5 per user per 60 minutes via the existing Redis sliding-window helper; on limit-hit returns 429 with a `Retry-After` header and audits `helpdesk.ticket_rate_limited`; on Redis fault logs and falls through fail-open so transient infra incidents don't block stuck users from asking for help)
MIGRATED: none — every fix lives in route-handler source. No schema delta, no new env contract, no new dependencies. The migrations directory next-idx (0018) remains reserved for the spec that actually touches the DB. `apps/web/package.json` is untouched.
