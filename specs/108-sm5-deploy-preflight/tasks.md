# Tasks 108

- [x] T1 → write governance test (red) → author `scripts/deploy.sh` with `set -euo pipefail` + check-restore-drill → docker compose up → curl-poll `/api/health` → migrate + seed_all → success echo → rewrite README-IT.md step 3 to call the wrapper and preserve original commands under "Manual fallback" → add top-level `Makefile` with `deploy:` target → green
