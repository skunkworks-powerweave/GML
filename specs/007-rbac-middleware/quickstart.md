# Quickstart 007

Visit `/dashboard` while logged out → redirected to `/login`.
Log in → `/dashboard` shows `Welcome <name> · role: <role>`.
Visit `/admin/data/teachers` as a teacher → forbidden page.
