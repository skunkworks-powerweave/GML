# Plan 140

CREATED: `specs/140-seed-forms-checkbox-fix/{spec,plan,research,quickstart,tasks}.md`, `tests/governance/test_140_seed_forms_checkbox_fix.test.mjs`
EDITED: `packages/db/src/scripts/seed_forms_mentor.ts` (rename `FieldKind` member `checkboxes` to singular `checkbox`, rename the `expertise_areas` field row's `kind` literal from plural to singular so the FormRenderer's `CheckboxGroup` branch matches, introduce `CANONICAL_FIELD_KINDS` allow-list and `assertCanonicalFieldKinds(forms)` runtime guard, wire the guard at the `FORMS` array declaration), `packages/db/src/scripts/seed_forms_mentee.ts` (introduce `CANONICAL_FIELD_KINDS`, `MENTEE_TYPE_TO_CANONICAL` mapping, and `assertCanonicalFieldKinds(rows)` guard; wire at the `ROWS` declaration), `packages/db/src/scripts/seed_forms_observation.ts` (introduce `CANONICAL_FIELD_KINDS`, `OBSERVATION_TYPE_TO_CANONICAL` mapping, and `assertCanonicalFieldKinds(templates)` guard; wire at the `TEMPLATES` declaration), `packages/db/src/scripts/seed_forms_misc.ts` (introduce `CANONICAL_FIELD_KINDS`, `MISC_KIND_TO_CANONICAL` mapping, and `assertCanonicalFieldKinds(rows)` guard; wire at the `ROWS` declaration)
MIGRATED: none — the bug is in the seed-emit layer, not in the schema. The `feedback_forms.schema` jsonb column shape is unchanged. Existing rows seeded with the old plural literal stay in place; a fresh seed run after this lands writes the correct singular kind. No new dependencies (pure TypeScript + the existing `console.warn` surface). No env contract. `seed_all.ts` (spec 104) still imports all four seeds in unchanged dependency order, so the orchestrator runbook from spec 104's quickstart needs no edit
