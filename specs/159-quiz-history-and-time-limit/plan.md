# Plan 159

CREATED: `specs/159-quiz-history-and-time-limit/{spec,plan,research,quickstart,tasks}.md`, `packages/db/src/migrations/0019_quiz_time_limit.sql` (ALTER TABLE ADD COLUMN `time_limit_seconds integer` plus ADD CONSTRAINT `quizzes_time_limit_range` CHECK enforcing `IS NULL OR BETWEEN 60 AND 7200`, with a rationale-bearing header comment explaining why NULL preserves the legacy untimed behaviour and why 60..7200 is the chosen range), `packages/db/src/migrations/meta/0019_snapshot.json` (carry-forward of 0018 with a fresh UUID `id`, `prevId` chains off 0018's id, `public.quizzes.columns` gains `time_limit_seconds`, `public.quizzes.checkConstraints` gains `quizzes_time_limit_range`), `apps/web/src/app/(authenticated)/quizzes/[slug]/history/page.tsx` (server component listing the current user's `quiz_submissions` rows for this quiz, newest-first, with role="table" markup, empty-state card carrying `data-testid="quiz-history-empty"`, populated state carrying `data-testid="quiz-history-table"`, per-row `data-testid="quiz-history-row"` and a "View result" link to `/quizzes/[slug]/result/[submissionId]`), `tests/governance/test_159_quiz_history_and_time_limit.test.mjs` (≥10 assertions: spec-kit presence, schema declares timeLimitSeconds field + quizzes_time_limit_range CHECK, migration SQL contains ADD COLUMN + ADD CONSTRAINT, snapshot chains prevId off 0018 + declares the new column and check, journal entry idx=19 between 18 and 20, QuizRunner + MobileQuizRunner accept the new prop and render data-testid countdown elements, history page exists with the table/empty data-testids, result page carries the View-history link, inline Spec 159 markers on every touched source file)

EDITED: `packages/db/src/schema/quizzes.ts` (adds `timeLimitSeconds: integer("time_limit_seconds")` to the quizzes table — NULLABLE so every legacy quiz stays untimed — plus a `check("quizzes_time_limit_range", ...)` constraint mirroring the migration's SQL clause; inline Spec 159 block above the new column documents the range rationale), `packages/db/src/migrations/meta/_journal.json` (inserts an `idx: 19, tag: "0019_quiz_time_limit"` entry between the existing idx-18 and idx-20 entries with a sequential `when` timestamp), `apps/web/src/components/quiz/QuizRunner.tsx` (adds `timeLimitSeconds?: number | null` prop; adds `formatRemaining(s)` MM:SS helper; adds a `useEffect` that mounts a `setInterval` countdown when the prop is set, ticks down to 0, and fires the same `submitAction(slug, answers)` shape as the manual click — every unanswered question carrying `selectedIndex: null` per spec 146; a `submittedRef` guards against a 0-second race so auto-submit + manual-click together fire exactly one server action; renders a `data-testid="quiz-countdown"` banner under the page header when `remaining` is non-null, shifting to `var(--rust)` under 60 seconds; teardown via the useEffect cleanup so navigation doesn't leak the interval), `apps/web/src/components/quiz/MobileQuizRunner.tsx` (same shape as the desktop runner — new prop, helper, effect, submittedRef — with the countdown rendered as a mono-font chip in the sticky header next to the `1 / N` counter, data-testid `mobile-quiz-countdown`), `apps/web/src/app/(authenticated)/quizzes/[slug]/page.tsx` (forwards `quiz.timeLimitSeconds` to both runners via the new prop), `apps/web/src/app/(authenticated)/admin/quizzes/[id]/page.tsx` (the `exportShape` returned to the JSON editor now carries `timeLimitSeconds`; a `data-testid="quiz-time-limit-summary"` line renders in the page header next to the pass-threshold readout; the schema-reference aside calls out the new field's contract), `apps/web/src/app/(authenticated)/admin/quizzes/[id]/actions.ts` (`IncomingPayload` gains `timeLimitSeconds?: number | null`; `saveQuizSchema` validates `null` or an integer in [60, 7200] — out-of-range values return `{ ok: false, error: "invalid_time_limit", message: ... }` rather than silently dropping the field; `updateSet.timeLimitSeconds` is written through to the transaction; the audit metadata for `quiz.schema.update` includes the new value), `apps/web/src/app/(authenticated)/quizzes/[slug]/result/[submissionId]/page.tsx` (the action-button row at the bottom of the result card adds a "View history" `<Link>` to `/quizzes/[slug]/history` with `data-testid="quiz-result-history-link"`)

MIGRATED: `0019_quiz_time_limit` — adds a NULLABLE `time_limit_seconds integer` column to `quizzes` plus a CHECK constraint enforcing `IS NULL OR BETWEEN 60 AND 7200`. Backwards-compatible (every existing row picks up NULL, satisfies the CHECK trivially, and the seed quizzes continue to render the untimed runner with no UI change). No data backfill needed. drizzle-kit wraps the statement in a transaction so we deliberately do NOT use CONCURRENTLY (incompatible with transactional DDL). No new dependencies. No env contract change. The migration sits at idx 19 in the journal; idx 20 is reserved for spec 161's parallel password-reset migration so the chain stays sequential across both audit-closure tracks. The history page rides the existing `quiz_submissions_user_idx` (user_id, submitted_at) — no new index needed.
