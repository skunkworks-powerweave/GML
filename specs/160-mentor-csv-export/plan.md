# Plan 160

CREATED: `apps/web/src/app/api/admin/data/mentors/export/route.ts` (GET handler that joins mentor_pairings filtered to status="active", emits a six-column CSV — id, name, hindiName, baseLocation, expertiseAreas (JSON-stringified), pairingsActive — with super_admin OR programme_admin gate, JSON 401/403/405 method matrix, audit row with action="mentors.bulk_export" after the SELECT so rowCount is accurate, and Content-Disposition: attachment; filename="mentors-YYYY-MM-DD.csv"), `specs/160-mentor-csv-export/{spec,plan,research,quickstart,tasks}.md`, `tests/governance/test_160_mentor_csv_export.test.mjs`
EDITED: `apps/web/src/app/(authenticated)/repo/mentors/page.tsx` (adds a `Download CSV` anchor in the page header — flex wrapper around the existing h1+p+label markup so the button sits flush-right of the heading column. Gated on `session.user.role === "super_admin" || session.user.role === "programme_admin"` so the `mentor` role doesn't see a dead link. `href="/api/admin/data/mentors/export"`, `className="btn btn-primary btn-sm"`, `data-testid="mentors-csv-export"` matches the existing `/repo/students` export-button pattern at lines 86-94)
MIGRATED: none — no schema change. The mentors + mentor_pairings tables already carry every column the export needs. Next migration index 0019 stays reserved for spec 159, 0020 for spec 161. No new dependencies — papaparse is already pulled in by the learners export and the generic admin CSV path
