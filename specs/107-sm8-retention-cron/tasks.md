# Tasks 107

- [x] T1 → write governance test (red) → refactor `retention.ts` to export `deleteOldNotifications()` + fix `../src/schema/...` typo → barrel from `packages/db/src/index.ts` → add `retention` Queue + Worker + nightly cron in `apps/worker/src/index.ts` → green
