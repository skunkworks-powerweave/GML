# Plan 155

CREATED: `specs/155-chrome-i18n-finish-and-forms-access/{spec,plan,research,quickstart,tasks}.md`, `tests/governance/test_155_chrome_i18n_finish_and_forms_access.test.mjs`, `apps/web/src/components/nav/LanguagePicker.tsx` (new `"use client"` island that owns the EN / हि / བོ switcher — receives the current locale as a prop, PUTs `{ uiLanguage }` to /api/user-prefs on selection, then forces a `window.location.reload()` so the NextIntlClientProvider in `(authenticated)/layout.tsx` re-mounts with the new messages bundle; pending state freezes the chip to `…`, error path surfaces an inline `role="alert"` row, outside-click closes the dropdown, picking the already-current locale is a no-op)

EDITED: `apps/web/src/components/nav/Topbar.tsx` (new optional `locale?: Locale` prop, the hardcoded `<details><summary>EN</summary>...</details>` block is replaced by `<LanguagePicker current={locale} ariaLabel={tLanguage("pickerLabel")} />` so the chip reflects the user's real `user_prefs.uiLanguage` and clicks land on the API), `apps/web/src/components/shells/DesktopShell.tsx` (forwards the same `locale` prop down to Topbar — MobileShell is out of scope, no parallel mobile switcher), `apps/web/src/app/(authenticated)/layout.tsx` (one-line wire-through: `<DesktopShell ... locale={locale}>` so the already-computed normalised locale reaches the picker), `apps/web/src/app/(authenticated)/forms/[slug]/page.tsx` (adds the `AUDIENCE_ALLOWED_ROLES` map + `isAudienceAccessAllowed` helper and an audit-and-redirect gate that runs immediately after the `feedbackForms` SELECT; the gate maps `audience: mentor → ["mentor"]`, `audience: mentee → ["teacher"]`, `audience: programme → "any"`, missing-audience → allowed, and on mismatch fires `void recordAudit({ action: "form.access.denied", entityType: "feedback_form", entityId: form.id, metadata: { slug, requiredAudience, role } })` before `redirect("/forbidden")`)

MIGRATED: none — both fixes are pure application logic. No schema change, no migration index consumed (next-idx 0018 remains reserved for spec 153). No new dependencies (the picker uses native `fetch` + `window.location.reload`; the gate reuses the existing `RoleName` enum and `recordAudit` helper). No env-contract change. The `audit_log.action` column is already `varchar(64)` since spec 021 so `form.access.denied` slots into the existing dotted-snake convention with no schema work.
