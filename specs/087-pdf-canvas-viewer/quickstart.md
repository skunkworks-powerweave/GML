# Quickstart 087

Visit `/repo/resource/<uuid>/view` as an authed user against a resource with a non-null `fileKey`; confirm the PDF renders in the iframe, the watermark overlay is visible diagonally, right-click does nothing, and `audit_log` gains a `resource.pdf.view` row.
