# Plan 141

CREATED: `specs/141-auth-fail-closed-and-gate-audit/{spec,plan,research,quickstart,tasks}.md`, `tests/governance/test_141_auth_fail_closed_and_gate_audit.test.mjs` (10+ assertions across the recordAudit signature change, the auth.ts fail-closed contract, the gate-actions audit emissions on success/fail/redis-down, plaintext-leak grep, and the audit-actions docs entry)
EDITED: `apps/web/src/lib/audit.ts` (recordAudit signature changes from `Promise<void>` to `Promise<boolean>` — true on commit, false on caught failure, never throws; adds optional `userId` + `ipOverride` inputs so the login flow can audit before the session exists; internal `auth()` / `headers()` calls are each wrapped in their own try/catch so a missing request scope downgrades to "insert without that field" rather than dropping the row), `apps/web/src/auth.ts` (imports `recordAudit`, adds private `maskIp(ip)` IPv4/IPv6 last-octet/hextet redactor, replaces the silent fail-open catch on the rate-limit call with: emit SEVERE `auth.rate_limit.redis_down` audit row, then `return null` — fail-closed; user-facing error stays the generic credential-failure copy so the degraded state is not leaked to attackers), `apps/web/src/app/gate/[slug]/actions.ts` (imports `recordAudit`, introduces `SERVICE_UNAVAILABLE` constant so the outage string lives in one place, tracks `rateLimited` and `attemptCount` across the action body; emits `gate.attempt.success` on the success path with `{ slug, attemptCount, rateLimited }` metadata; emits `gate.attempt.fail` on wrong-password and rate-limit-exceeded paths with `reason` metadata; emits SEVERE `gate.rate_limit.redis_down` on Redis fault and returns generic `Service temporarily unavailable.` — fail-closed), `docs/audit-actions.md` (the `gate.*` row gains `gate.rate_limit.redis_down`; a new `auth.*` row documents `auth.rate_limit.redis_down` with the spec 141 cross-ref)
MIGRATED: none — `audit_log.action` is already `varchar(64)` free-form since spec 021, so the new dotted-notation actions need no schema change; no new dependencies (uses only `recordAudit`, `rateLimit`, and standard Node string ops); no env contract; the `Promise<boolean>` return type is additive — `void recordAudit(...)` callers across 58 files stay correct and discard the boolean
