# Plan 146

CREATED: `specs/146-quiz-grading-fix/{spec,plan,research,quickstart,tasks}.md`, `tests/governance/test_146_quiz_grading_fix.test.mjs`

EDITED: `apps/web/src/components/quiz/QuizRunner.tsx` (widen `submitAction` prop type to `selectedIndex: number | null`; rewrite the `onSubmit` body to map over the full `questions` array and emit `null` for any skipped question — no more `.filter` shrinking the wire payload), `apps/web/src/components/quiz/MobileQuizRunner.tsx` (same two changes — widened type + full question list with null for skipped, drop-in identical contract with desktop runner per spec 134), `apps/web/src/app/(authenticated)/quizzes/[slug]/page.tsx` (widen `submitQuizAttempt`'s `answers` param type to `Array<{ questionId: string; selectedIndex: number | null }>`; rewrite the grading loop to track `answeredCount` separately from `correct`; emit `answeredCount` in the `quiz.submit` audit metadata so the trail can distinguish "answered X / N correctly" from "skipped"; the percentage rule itself is unchanged — `correct / qs.length`, with skipped counting as wrong), `apps/web/src/app/(authenticated)/quizzes/[slug]/result/[submissionId]/page.tsx` (render an `X of N answered · Y correct of N · Z skipped` breakdown via `data-testid="quiz-result-breakdown"`; per-question card treats `null` and `undefined` identically as "skipped" with the more honest "Skipped" copy in place of "No answer"; correctness icon stays red × for both wrong-and-skipped because the percentage rule treats them the same), `packages/db/src/schema/quizzes.ts` (widen `quizSubmissions.answers` `$type<>` annotation to `Array<{ questionId: string; selectedIndex: number | null }>` so the TS type at write and read sites matches the new client contract — jsonb storage is unchanged), `tests/governance/test_134_mobile_quiz_runner.test.mjs` (update the two `selectedIndex: number` regexes to accept `number | null` so spec 134's contract assertions continue to pin the mobile runner without contradicting spec 146)

MIGRATED: none — the change is purely a type-level widening on a jsonb column. No SQL migration, no new env var, no new dependency. Pre-146 `quiz_submissions` rows continue to round-trip safely because the result page treats any missing question entry as "skipped", same as a null entry would be. The `quiz.submit` audit emits one additional metadata field (`answeredCount`); existing audit consumers will simply ignore the new field.
