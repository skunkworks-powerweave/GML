# Quickstart 088

Log in on any device → press Ctrl+S, Ctrl+P, or PrintScreen on any authenticated page → save/print dialog suppressed and `anti_download.attempt.*` rows land in `audit_log`; trigger DevTools → one `anti_download.devtools.detected` row per session.
