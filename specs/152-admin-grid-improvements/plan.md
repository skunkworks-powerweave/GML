# Plan 152

CREATED: `specs/152-admin-grid-improvements/{spec,plan,research,quickstart,tasks}.md`, `tests/governance/test_152_admin_grid_improvements.test.mjs`
EDITED: `apps/web/src/app/(authenticated)/admin/data/[entity]/page.tsx` (introduces `unwrapZod` + `buildColumnFilter` helpers that dispatch the filter by the column's Zod type — ZodString → ilike, ZodEnum → eq with options-membership guard, ZodBoolean → eq with "true" coercion, ZodNumber → eq with finite-number guard, anything else → skip with dev-mode warn. The filter loop now calls the dispatcher and appends to `whereClauses` only when the result is non-null; rejected keys accumulate in `skippedFilters` which is folded into the SM-9 PII-audit row so user intent is still recorded even when the filter doesn't reach the DB), `apps/web/src/app/api/admin/forms/[id]/route.ts` (wraps the SELECT + UPDATE pair in `db.transaction(async (tx) => { ... })` and acquires a row-level lock via `.for("update")`; the transaction returns the resolved (prevVersion, nextVersion) pair or null for not-found; `recordAudit("form.schema.update")` fires AFTER the transaction commits, matching the spec-148 gate-rotate pattern — audit failure can never roll back a committed bump)
MIGRATED: none — neither fix touches schema. The unique index on `feedback_forms (kind, audience, version)` already exists from spec 073; the transaction is the suspenders to the belt of the index. No new dependencies — `db.transaction` + `.for("update")` are already in `drizzle-orm/node-postgres` (used by spec 148 for the same shape). No env contract changes. Next migration idx (0018) remains reserved for spec 153
