# Research 017
- `sessions` (bare slug freed by spec 013) for classroom; RTT side is `rtt_sessions`.
- status as varchar+CHECK for symmetry with course_outlines+classes.
- observation_cycle_id nullable — only populates when observer schedules a cycle against the session.
