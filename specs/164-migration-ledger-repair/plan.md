# Plan 164

CREATED: `specs/164-migration-ledger-repair/{spec,plan,research,quickstart,tasks}.md`, `tests/governance/test_164_migration_ledger_repair.test.mjs`, `packages/db/src/migrations/meta/0020_snapshot.json` (drizzle-kit snapshot capturing schema state AFTER `0020_password_reset_and_lockout.sql` — password_reset_tokens table with the cascade FK + the two indexes, users.failed_login_count default 0, users.locked_until nullable — but BEFORE `0021_transcode_jobs_dropped_status.sql` so the `transcode_jobs_status_check` constraint still reads `IN ('queued','running','succeeded','failed','cancelled')` without `'dropped'`; `prevId` chains back to `0019_snapshot.json`'s id; new top-level `id` is a fresh uuid that `0021_snapshot.json` chains forward from), `packages/db/src/migrations/meta/0021_snapshot.json` (drizzle-kit snapshot capturing schema state AFTER `0021_transcode_jobs_dropped_status.sql` — the ONLY delta from `0020_snapshot.json` is that the `transcode_jobs_status_check` constraint gains `'dropped'` as a valid status, matching the runtime SQL); the snapshot was first produced via `pnpm --filter @gml/db generate` to capture the live schema state, then the `prevId` was patched to point to the new `0020_snapshot.json` id so the snapshot chain `0019 → 0020 → 0021` is continuous
EDITED: `packages/db/src/migrations/meta/_journal.json` (appended `{ idx: 21, version: "7", when: 1780716000000, tag: "0021_transcode_jobs_dropped_status", breakpoints: true }` so the runtime migrator sees the manual `0021_*.sql` file as in-ledger; the `when` timestamp 1780716000000 is intentionally slightly past 0020's 1780672800000 so the chronological order matches the idx order; `breakpoints: true` matches every adjacent entry — drizzle-kit's `--> statement-breakpoint` markers in the SQL files require it)
MIGRATED: none — the repair is purely in the drizzle-kit bookkeeping ledger under `packages/db/src/migrations/meta/`. The two `.sql` files under `packages/db/src/migrations/` (`0020_password_reset_and_lockout.sql` for spec 161 and `0021_transcode_jobs_dropped_status.sql` for spec 162) are UNCHANGED — they were authored correctly, they just weren't registered in the snapshot chain or the journal. The production migrator (`packages/db/scripts/migrate.ts`, which uses `drizzle-orm/node-postgres/migrator`) reads `_journal.json` only at runtime; the `*_snapshot.json` files are dev-time-only artefacts consumed by `drizzle-kit generate` to compute the diff against the live schema. No new dependencies, no schema edit, no API change
