# Plan 142

CREATED: `specs/142-form-renderer-action-prop-and-validation/{spec,plan,research,quickstart,tasks}.md`, `tests/governance/test_142_form_renderer_action_prop_and_validation.test.mjs`
EDITED: `apps/web/src/components/forms/FormRenderer.tsx` (wires the previously-ignored `action` prop onto the `<form>` element so the forms-runner server-action submit path actually fires; adds a dev-mode `console.error` when both `action` and `onSubmit` are wired; tightens the validation gate to use the local `errs` constant; sets `submitting` before any await and resets it in `finally` on the onSubmit path; adds `data-testid="form-renderer-submit"` on the submit button so the governance suite can pin the disabled-on-submit state), `apps/web/src/components/forms/MobileFormRunner.tsx` (adds the `onSubmit` prop and mirrors the desktop both-set assertion; the rapid double-tap submit path now early-returns when `submitting` is already true and uses the local `errs` constant for the validation gate)
MIGRATED: none — both fixes live entirely in the React component tree. Zero SQL, zero env, zero new dependencies. Next migration index (0016) is reserved for a different spec in this Workflow Run 13 audit-closure batch
