# Quickstart 073

Sign in as `programme_admin`, navigate to `/admin/forms` → click any form row → edit the JSON in the left textarea → click **Save** → the version chip updates and `audit_log` shows a `form.schema.update` row.
