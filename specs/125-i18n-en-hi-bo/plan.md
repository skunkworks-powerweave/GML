# Plan 125

CREATED: `specs/125-i18n-en-hi-bo/{spec,plan,research,quickstart,tasks}.md`, `tests/governance/test_125_i18n_en_hi_bo.test.mjs`, `apps/web/src/i18n/config.ts`, `apps/web/src/i18n/locales/{en,hi,bo}.json`, `apps/web/src/app/login/layout.tsx`, `apps/web/src/app/login/language-picker.tsx`, `apps/web/src/app/gate/[slug]/gate-form.tsx`
EDITED: `apps/web/package.json` (add next-intl ^4.13.0 dependency), `apps/web/src/app/(authenticated)/layout.tsx` (read user_prefs.uiLanguage + wrap children in NextIntlClientProvider + apply --deva font for hi), `apps/web/src/components/nav/Topbar.tsx` (async, getTranslations for sign-out title + bell aria-label + language picker), `apps/web/src/components/nav/Sidebar.tsx` (async, getTranslations for section headings + nav item labels + Online status), `apps/web/src/components/nav/BottomTabs.tsx` (async, getTranslations for mobile tab labels), `apps/web/src/app/gate/[slug]/page.tsx` (convert to async server component, getTranslations, NextIntlClientProvider, split form into gate-form.tsx island), `apps/web/src/app/login/page.tsx` (useTranslations for sign-in / welcome / password / magic link / forgot, mount LoginLanguagePicker), `apps/web/src/app/(authenticated)/dashboard/page.tsx` (getTranslations for greeting + What's next/Today + Confidentiality headings)
MIGRATED: none — i18n is a runtime concern; the `user_prefs.ui_language` column already exists and accepts en/hi/bo via the spec 024 CHECK constraint
