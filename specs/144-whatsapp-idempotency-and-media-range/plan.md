# Plan 144

CREATED: `specs/144-whatsapp-idempotency-and-media-range/{spec,plan,research,quickstart,tasks}.md`, `packages/db/src/migrations/0017_whatsapp_dedup.sql` (two-statement ALTER TABLE + partial UNIQUE INDEX migration), `packages/db/src/migrations/meta/0017_snapshot.json` (cumulative snapshot with the new column + partial unique index baked into `public.video_submissions`), `tests/governance/test_144_whatsapp_idempotency_and_media_range.test.mjs` (≥10 assertions: column exists, partial unique index uses `WHERE … IS NOT NULL`, journal entry present, webhook pre-checks, webhook insert sets the column, webhook uses `onConflictDoNothing`, replay audit action exists, media route reads incoming `Range`, forwards it to S3, returns 206 with `Content-Range`, advertises `Accept-Ranges` on the 200 branch)
EDITED: `packages/db/src/schema/videos.ts` (adds `whatsappMessageId` column + partial unique index with `WHERE … IS NOT NULL` predicate), `packages/db/src/migrations/meta/_journal.json` (adds the 0017 journal entry), `apps/web/src/app/api/webhooks/whatsapp/route.ts` (pre-check SELECT for `whatsappMessageId = msg.id` with `whatsapp.message.replay_ignored` audit + `whatsappMessageId: msg.id` on the insert + `onConflictDoNothing` for the race-window between pre-check and DB write), `apps/web/src/app/api/media/[token]/route.ts` (reads incoming `Range`, forwards to `GetObjectCommand({ Range })`, returns 206 + `Content-Range` + `Content-Length` + `Accept-Ranges: bytes` on the range branch; no-range path adds `Accept-Ranges: bytes` so HLS.js can probe)
MIGRATED: `0017_whatsapp_dedup` — adds `video_submissions.whatsapp_message_id text` (nullable) and the partial `CREATE UNIQUE INDEX video_submissions_whatsapp_message_id_uq ON video_submissions(whatsapp_message_id) WHERE whatsapp_message_id IS NOT NULL`. Backwards-compatible (existing rows are NULL and exempt from the unique constraint). No new dependencies; PostgreSQL handles partial unique indexes natively. No env contract change. SM-3 (video-original retention) untouched. SM-7 (mobile-bandwidth budget) materially helped by the Range pass-through — segment reseeks no longer re-download from byte 0
