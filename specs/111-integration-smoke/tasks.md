# Tasks 111

- [x] T1 → write governance test asserting smoke file shape (red) → create `tests/integration/smoke.test.mjs` with 8 fetch-based tests + AbortController probe + per-test skip → narrow `package.json` `test` glob to governance, add `test:smoke` script → green
