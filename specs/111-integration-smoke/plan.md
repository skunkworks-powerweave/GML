# Plan 111

CREATED: `specs/111-integration-smoke/{spec,plan,research,quickstart,tasks}.md`, `tests/integration/smoke.test.mjs`, `tests/governance/test_111_integration_smoke.test.mjs`
EDITED: `package.json` (narrow `test` glob to `tests/governance/`, add `test:smoke` running `node --test tests/integration/**/*.test.mjs`)
MIGRATED: none
