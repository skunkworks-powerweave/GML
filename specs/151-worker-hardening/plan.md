# Plan 151

CREATED: `specs/151-worker-hardening/{spec,plan,research,quickstart,tasks}.md`, `tests/governance/test_151_worker_hardening.test.mjs`
EDITED: `apps/worker/src/queues.ts` (introduces a shared `defaultJobOptions` const with `attempts: 3`, exponential backoff `delay: 5000`, `removeOnComplete` `{ age: 24*3600, count: 100 }`, and `removeOnFail` `{ age: 7*24*3600 }`; passes the same defaults to BOTH `new Queue("transcode", ...)` and `new Queue("retention", ...)` so every producer site inherits the retry policy without having to remember it — closes the Workflow Run 14 audit finding that a single ffmpeg / MinIO blip on a 3G uplink would permanently lose a teacher's video), `apps/worker/src/index.ts` (replaces the unbounded `parseInt(env.WORKER_CONCURRENCY)` with `Math.max(1, Math.min(parseInt(env ?? "2", 10) || 2, 16))` so a typo'd env value can't disable the worker, crash it at startup, or OOM-kill it; adds a comment block above the `retentionQueue.add(...)` call documenting that `cron "0 3 * * *"` evaluates in server-local TZ which is UTC in production = 08:30 IST, with a `TZ` env override escape hatch)
MIGRATED: none — worker-only behavioural fix, no schema delta, no env contract addition (WORKER_CONCURRENCY already documented; the clamp is purely defensive). No new dependencies (no @bull-board/api, no cron-parser — both clamp and timezone fix are inline JS). Next migration idx (0018) stays reserved for spec 153
