# Quickstart 049
Open `/repo/subjects` → table of 9 subjects with roll-up counts; click a row → `/repo/subject/[id]` renders the 4-stat strip + outlines + recent sessions + readings (matches JSX lines 444-560).
