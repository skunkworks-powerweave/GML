# Plan 158

CREATED: `specs/158-repo-search-bars/{spec,plan,research,quickstart,tasks}.md`, `tests/governance/test_158_repo_search_bars.test.mjs`
EDITED: `apps/web/src/app/(authenticated)/repo/schools/page.tsx` (adds `?q=` SearchParams field, `SEARCH_Q_MAX`/`escapeIlike` helpers, `ilike(schools.name, %q%)` combined with the existing district WHERE via `and(...)`, inline `<form method="GET">` search input + Clear link, q-preserving district tab hrefs), `apps/web/src/app/(authenticated)/repo/teachers/page.tsx` (adds `q?: string` to SearchParams, the helpers, `ilike(teachers.fullName, %q%)` appended to the existing school+phase conds array, Name input as the first control in the existing filter form, Reset button renamed Clear and triggered by any of school/phase/q), `apps/web/src/app/(authenticated)/repo/mentors/page.tsx` (adds `searchParams` prop where the page had none, `ilike(mentors.name, %q%)` joins the active gate via `and(...)`, a new filter card with the search input is added at the top of the page body), `apps/web/src/app/(authenticated)/repo/subjects/page.tsx` (adds `q?: string`, the helpers, `ilike(subjects.name, %q%)` joins the grade-range conds array, Name input as the first control in the existing form, Reset renamed Clear), `apps/web/src/app/(authenticated)/repo/sessions/page.tsx` (adds `q?: string` to SearchParams, helpers, `ilike(sessions.topic, %q%)` joins existing status/subject/from/to conds array, topic search input added to the existing form, q-preserving status-tab hrefs, Clear link), `apps/web/src/app/(authenticated)/repo/resources/page.tsx` (adds `q?: string` to searchParams, helpers, `ilike(resources.name, %q%)` joins the existing kind filter via and(...), new search-bar card sits above the existing kind-pill row, q-preserving kind-pill hrefs), `apps/web/src/app/(authenticated)/repo/outlines/page.tsx` (adds `q?: string`, helpers, `ilike(courseOutlines.name, %q%)` joins grade/term/status conds array, Name input as the first control, Reset renamed Clear)
MIGRATED: none — pure ILIKE-on-existing-columns query change; no schema delta, next migration idx 0019 still reserved for spec 159 and 0020 for spec 161. No new dependencies (no fuzzy-search lib, no client-side debounce hook). No env contract changes. The 7 repo index pages share the literal `SEARCH_Q_MAX = 200` constant and `escapeIlike(s)` helper — copy-paste rather than extraction so each page stays self-contained and the governance test can pin each occurrence independently
