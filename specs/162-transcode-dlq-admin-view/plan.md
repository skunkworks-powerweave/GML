# Plan 162

CREATED: `specs/162-transcode-dlq-admin-view/{spec,plan,research,quickstart,tasks}.md`, `tests/governance/test_162_transcode_dlq_admin_view.test.mjs`, `apps/web/src/app/(authenticated)/admin/transcode-jobs/page.tsx` (server component, force-dynamic, programme_admin + super_admin gate, BullMQ getJobCounts top strip, URL-driven filter pills All/Failed/In progress/Queued/Recent-24h, status-sorted join of transcode_jobs ⨝ video_submissions, Retry + Drop forms wired to server actions on failed rows only, `transcode.dlq.surface_viewed` audit on render), `apps/web/src/app/(authenticated)/admin/transcode-jobs/actions.ts` (`"use server"`, retryTranscodeJobAction re-enqueues onto transcodeQueue with the five-field webhook payload and records `transcode.retry_requested`, dropTranscodeJobAction sets transcode_jobs.status='dropped' + flips parent video_submissions.status='failed' and records `transcode.dropped`, both share the programme_admin + super_admin gate and validate the row is `status='failed'` before mutating), `packages/db/src/migrations/0021_transcode_jobs_dropped_status.sql` (DROP + ADD the `transcode_jobs_status_check` CHECK to widen the status set with `'dropped'`)
EDITED: `packages/db/src/schema/videos.ts` (CHECK constraint on transcode_jobs.status widened to include `'dropped'` alongside the existing five statuses; inline comment explains the operator-verb semantics distinct from `cancelled`), `apps/web/src/app/(authenticated)/admin/page.tsx` (new entry in the System section linking to `/admin/transcode-jobs` so operators can navigate to the DLQ surface from the admin index)
MIGRATED: 0021_transcode_jobs_dropped_status — widens `transcode_jobs_status_check` to include `'dropped'`. Single-transaction DROP + ADD CONSTRAINT, no data migration (existing rows carry one of the five legacy statuses, all valid under the new CHECK). Index 0021 is workflow-coordinated (0019 reserved for spec 159, 0020 for spec 161, 0021 for this spec 162). No new dependencies — reuses transcodeQueue from `@gml/worker/queues`, requireRole from `@/lib/guards`, recordAudit from `@/lib/audit`, db from `@gml/db`. No env contract change. No client component added (page is a pure server component using `<form action={...}>` for the verbs)
