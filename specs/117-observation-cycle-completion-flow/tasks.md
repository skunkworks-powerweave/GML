# Tasks 117

- [x] T1 → author governance test (red) covering the 6 CTAs → write `actions.ts` with 5 server actions + `transitionCycleStatus` helper → edit `page.tsx` to import actions, mount inline forms gated on cycle status, mount `<UploadProgress>` with observation_cycle context → green (governance test passes, files exist, audit hooks present, role gates present, atomic transitions with `.returning`).
