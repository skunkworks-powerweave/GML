# Tasks 120

- [x] T1 → write governance test (red) → add 3 quiz tables + barrel export → write 0014 migration SQL + journal + snapshot → green schema layer
- [x] T2 → ship `<QuizRunner>` client component + `/quizzes/[slug]` server page + `submitQuizAttempt` server action → grade + audit + redirect
- [x] T3 → ship `/quizzes/[slug]/result/[submissionId]` page with per-question breakdown + pass/fail banner
- [x] T4 → ship `/admin/quizzes` index + `/admin/quizzes/[id]` JSON editor + `saveQuizSchema` server action + audit hook → green
